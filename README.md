# smart-agri (minimal starter)

Run locally:
1. Install dependencies (root, frontend, backend).
2. Start both dev servers with `npm run dev`.

This repo contains a minimal React frontend (Vite) and Express backend (in-memory demo).
